apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: helloworld
  name: helloworld
spec:
  port:
    targetPort: web
  to:
    kind: Service
    name: helloworld
    weight: 100
  tls:
    termination: edge
  wildcardPolicy: None
